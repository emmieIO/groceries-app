import { ScreenWrapper } from "@/components/screen-wrapper";
import { Text } from "react-native";

export default function ListScreen(){
    return (
        <ScreenWrapper>
            <Text>Welcome to the lists Screen</Text>
        </ScreenWrapper>
    )
}